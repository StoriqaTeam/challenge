<%= form_with(model: suite_solution, url: user_suite_solutions_path(@user), local: true) do |form| %>
  <% if suite_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suite.errors.count, "error") %> prohibited this suite solution from being saved:</h2>

      <ul>
      <% suite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
  <%= form.select :suite_id, @suite_options %>
  <%= label_tag(:suite_id, "Suite") %>
</div>

  <div class="actions">
    <%= form.submit "Submit", class: "btn" %>
  </div>
<% end %>
