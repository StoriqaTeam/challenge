<p id="notice" class="hide"><%= notice %></p>

<h3>Tasks for suite `<%= @suite.name %>`</h3>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Priority</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @suite_tasks.each do |suite_task| %>
      <tr>
        <td><%= suite_task.task.title %></td>
        <td><%= suite_task.priority %></td>
        <td>
          <%= link_to 'Delete', suite_task_path(@suite, suite_task.task), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn red" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to new_suite_task_path, class: "btn-floating btn-large waves-effect waves-light red" do %>
  <i class="material-icons">add</i>
<% end %>
